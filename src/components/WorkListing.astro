---
const { name, description, typography } = Astro.props;
---
<div id={name} class="col-span-2 flex flex-col gap-10 items-center my-40">
  <div class="text-center text-4xl md:text-7xl font-serif font-medium tracking-[-0.03em] ">
    {name}
  </div>
  <div class={`mx-12 lg:mx-0 md:${typography} text-xs md:text-base mb-3 text-center`}>
    {description}
  </div>
</div>
